let primary = ["red", "green", "blue"];
let secondary = ["yellow", "orange", "pink"];

primary.concat(secondary);
primary
secondary

let allColors = primary.concat(secondary);
allColors

secondary.concat(primary);


primary.reverse();
secondary.reverse();